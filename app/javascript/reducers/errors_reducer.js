const ErrorsReducer = (oldState = {}, action) => {
    return oldState;
    // switch (action.type) {
    //     case RECEIVE_ERROR:
    //         return action.errors;
    //     default:
    //         return oldState;
    // }
}

export default ErrorsReducer;